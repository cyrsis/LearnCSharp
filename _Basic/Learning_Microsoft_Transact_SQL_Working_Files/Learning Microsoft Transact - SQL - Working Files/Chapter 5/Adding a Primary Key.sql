How schemas affect your T-SQL code in SQL Server
A user can access objects within their default schema without referencing the schema name in code
The schema name must be included if the query affects a schema that is not the user’s default schema
We’ll look at working with schemas in T-SQL code in a separate video
“Working with Schemas”
